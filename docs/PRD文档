# ClickDeck PRD（结构化修订版）

## 0. 当前判断

Chrome 插件调研文档和本 PRD 说的是同一个大方向：让用户不进入 F12、不写 CSS，也能对已有 HTML/网页内容做可视化微调。

两份文档的共同点是“在用户已经看到的 HTML 页面上做最后一公里微调”。差异只在于是否要把改动写回源码：

- Chrome 插件文档：包含“在浏览器里点选页面元素，并把改动精准写回本地源码”的工程增强路线。
- 本 PRD：聚焦“在 Chrome 当前页面上直接微调，并导出 PDF/HTML”的交付闭环。

建议统一为一个产品叙事：

ClickDeck 是一个运行在 Chrome 里的 HTML 视觉语义编辑器。MVP 先完成“打开已有 HTML 页面 -> 在当前页面直接微调 -> 导出 PDF/HTML”的闭环；PPT 导出进入可研和后续版本；源码回写能力作为后续专业增强路线。

## 1. 产品边界

### 1.1 ClickDeck 不做什么

- 不负责 AI 生成内容。
- 不接入 LLM。
- 不提供 prompt、AI 写作或内容生产能力。
- MVP 不做完整设计软件。
- MVP 不做自由画布拖拽系统。
- MVP 不承诺精准回写 React/Vue/Slidev/Markdown 源码。

### 1.2 ClickDeck 只做什么

把 Chrome 里已经打开的 HTML/网页内容变成可视化、可微调、可导出 PDF/HTML 的编辑系统。

## 2. 一句话定义

ClickDeck 是一个 Chrome 插件，让用户在已经打开的 HTML 页面上直接点选内容，通过“字号、间距、对齐、强调、颜色”等语义化控制完成视觉微调，并快速导出 PDF 和 HTML。

## 3. 产品本质

不是 AI 生成工具。

不是 Figma 或 Webflow 式设计软件。

而是：

```text
Chrome 当前页面 / 已有 HTML / 网页 DOM
        ↓
ClickDeck 页面覆盖层
        ↓
视觉语义微调
        ↓
PDF / HTML 导出
```

## 4. 目标用户

### 4.1 核心用户

- 用 AI、模板或代码工具生成了 HTML 演示文稿，但不想继续改代码的人。
- 产品经理、运营、销售、培训、咨询等需要快速修改展示材料的人。
- 已经在 Chrome 里预览页面，希望就地改完并导出的人。

### 4.2 次级用户

- 开发者和 DevRel，希望快速微调 HTML slides 或 demo 页面。
- 团队内负责把网页内容转成汇报材料的人。

## 5. 要解决的问题

当前 HTML 内容修改存在三个核心问题：

1. 用户需要进入 F12、源码或 CSS 层才能改样式。
2. CSS 参数（px、rem、hex、class name）不符合普通用户心智。
3. 修改、预览、导出之间割裂，无法像 PPT 一样快速试错。

## 6. 产品目标

- 将 HTML 转换为可选择、可编辑的视觉对象。
- 将 CSS 参数转化为用户能理解的语义控制。
- 提供即时视觉反馈，避免反复刷新和重新生成。
- 导出可展示的 PDF 和可继续使用的 HTML。
- 不要求回写原始源码，但要能保存当前页面的修改结果。

## 7. MVP 范围

### 7.1 输入

MVP 支持：

- 在 Chrome 当前页面启用 ClickDeck。
- 支持本地 HTML 页面、localhost 预览页面、普通网页页面。
- 从当前页面 DOM 读取可编辑结构。

暂不承诺：

- 直接解析复杂前端工程源码。
- 直接回写 React/Vue/Slidev/Markdown 源文件。
- 对任意第三方网站都保持 100% 可编辑和 100% 导出还原。

### 7.2 核心能力

#### A. HTML 解析

将当前页面解析为：

```text
Chrome 当前页面 DOM
   ↓
可编辑元素/区块识别
   ↓
Visual State Layer
```

基础 block 类型包括：

- 标题
- 正文
- 图片
- 列表
- 表格
- 分组容器
- 幻灯片/页面容器

#### B. 可视化编辑

用户只操作语义化选项，不直接接触 CSS：

| 控制项 | MVP 选项 |
| --- | --- |
| 字号 | 小 / 中 / 大 / 更大 |
| 间距 | 紧凑 / 标准 / 宽松 |
| 对齐 | 左 / 居中 / 右 |
| 强调 | 弱化 / 正常 / 强调 |
| 颜色 | 主文本 / 次文本 / 弱文本 / 强调色 |
| 图片 | 替换 / 适应 / 填充 |
| 区块顺序 | 上移 / 下移 |

#### C. 实时渲染

- 所有修改即时生效。
- 不刷新页面。
- 不重新生成内容。
- 以状态更新和 CSS 变量更新为主。

#### D. 导出

MVP 导出：

- PDF：第一优先级，基于打印样式导出，目标是可以直接展示、分享、归档。
- HTML：第二优先级，导出当前修改后的 DOM 快照和 ClickDeck 样式映射，目标是可继续打开和展示。

PDF 导出模式：

- 长页面：优先保证可用，适合网页、报告、长文档。
- 16:9 演示页：适合 HTML slides，优先识别 section、slide、page 等页面容器。
- A4 文档：适合正式文档和打印，重点处理页边距和分页截断。

MVP 可以在界面上提供三种模式选择，但工程优先级是先保证长页面稳定，再优化 16:9 和 A4 的分页质量。

PPT 导出暂不进入 MVP 承诺。它需要单独可研：如果只导出“图片型 PPT”，实现简单但不可编辑；如果导出“可编辑 PPT”，需要把 HTML/CSS 布局映射成 PowerPoint 的文本框、图片、形状和坐标系统，复杂度明显更高。

## 8. 不做自由拖拽的原因

MVP 不做 Figma 式自由拖拽系统。

原因：

- DOM 布局与自由坐标系统天然冲突。
- 需要引入绝对定位、碰撞、响应式约束和层级管理。
- 会把产品复杂度推向设计软件，而不是“HTML 微调工具”。

替代方案：

- 区块上移/下移。
- 区块合并/拆分。
- 图片适应/填充。
- 内容宽度、间距、对齐的语义化调整。

## 9. 技术架构

### 9.1 MVP 架构

```text
Chrome 当前页面
   ↓
Content Script 注入
   ↓
元素选择与编辑覆盖层
   ↓
Visual State Layer
   ↓
DOM/CSS 实时应用
   ↓
PDF/HTML Export Pipeline
```

### 9.2 Visual State 示例

```json
{
  "document": {
    "pages": [
      {
        "id": "page-1",
        "blocks": [
          {
            "id": "block-1",
            "type": "heading",
            "contentRef": "node-1",
            "style": {
              "size": "large",
              "spacing": "standard",
              "align": "center",
              "emphasis": "strong",
              "color": "primaryText"
            }
          }
        ]
      }
    ]
  }
}
```

### 9.3 样式实现原则

- 状态层保存语义 token。
- 渲染层把 token 转成 CSS variables 或 class。
- 导出层把最终状态写入 PDF/HTML。
- 不把用户暴露在 CSS 属性、选择器权重和像素值里。

## 10. 源码回写路线

Chrome 插件是 MVP 形态，源码回写不是 MVP。

### 10.1 两种形态的关系

| 形态 | 目标 | 适合阶段 |
| --- | --- | --- |
| Chrome 插件页面微调 | 在当前网页上点选、微调、导出 PDF/HTML | MVP |
| 源码回写桥接 | 把改动精准写回本地工程源码 | P2/专业版 |

### 10.2 为什么不把源码回写放进 MVP

- 需要处理 React/Vue/Slidev/Markdown 等源码映射。
- 需要 AST、source map、本地桥接服务或构建插件。
- 技术风险和兼容成本高。
- 会把 MVP 从“Chrome 页面微调导出工具”拉成“前端工程编辑基础设施”。

### 10.3 可保留的 Chrome 插件文档结论

- 用户确实需要替代 F12 的“点哪改哪”体验。
- 视觉编辑工具已有需求验证，如 VisBug、Stylebot、Amino。
- 真正难点是持久化和结构安全，不只是改 DOM。
- 如果未来做源码回写，应优先考虑 AST/source map/本地桥接，而不是正则替换源码。

### 10.4 开源复用判断

如果存在成熟、许可合适、架构可拆的开源项目，源码回写能力可以提前探索，但仍不建议阻塞 MVP。

原因：

- 开源项目通常能降低“交互面板、元素选择、样式编辑、代码映射”的研发成本。
- 但它们往往绑定特定技术栈，例如 React、Next.js、Tailwind 或本地开发容器。
- ClickDeck 的第一目标是“在 Chrome 当前页面微调后导出 PDF/HTML”，不是“改工程源码”。
- 因此更合适的策略是先完成 Chrome 插件导出型 MVP，同时并行调研是否能复用 Onlook、VibeLens、VisBug、Stylebot 等项目的局部能力。

## 11. MVP 成功标准

### 11.1 功能标准

- 用户可以在 Chrome 当前页面启用 ClickDeck。
- 系统可以识别基础文本、图片、列表、容器和页面结构。
- 用户可以通过语义化控件调整字号、间距、对齐、强调和颜色。
- 修改能实时反馈。
- 用户可以导出 PDF 和 HTML，PDF 至少支持长页面、16:9、A4 三种模式入口。

### 11.2 用户行为标准

- 用户不需要打开 F12。
- 用户不需要理解 px、hex、class、selector。
- 用户可以在 5 分钟内完成一次“打开 HTML 页面 -> 微调 -> 导出 PDF”。

### 11.3 技术标准

- 常规编辑响应延迟小于 100ms。
- 编辑后 DOM 结构稳定，不出现明显内容丢失。
- 导出的 HTML 能独立打开。
- PDF 长页面模式稳定可用；16:9 和 A4 模式具备基础分页能力。

## 12. 主要风险

1. 网页 DOM 来源太复杂，导致元素识别和导出质量不稳定。
2. PPT 导出天然无法完整表达 CSS/Flex/Grid/动画等 Web 能力，暂不作为 MVP 承诺。
3. “语义化控制”如果太少，会显得不够可控；如果太多，又会变成 CSS 面板。
4. 如果用户真实需求是“改源码”，Chrome 内导出型 MVP 可能不够有吸引力。
5. 如果用户真实需求是“网页转 PPT”，产品重心需要重新向 PPT 映射质量倾斜。

## 13. 需要进一步确认的问题

1. 用户最常见的使用场景是本地 HTML 文件、localhost 页面，还是线上网页？
2. HTML 微调主要发生在整页级、区块级，还是文字/图片元素级？
3. PPT 导出是否只是后续加分项，还是未来必须进入主线？
4. 修改是否需要跨刷新保留，还是只要当前会话可编辑并能导出即可？

## 14. 当前产品一句话

ClickDeck 是一个运行在 Chrome 里的 HTML 视觉语义编辑器，让用户在当前页面上通过“感觉式操作”替代代码修改，并快速导出可展示的 PDF 和 HTML。
